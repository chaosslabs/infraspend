apiVersion: v1
kind: Service
metadata:
  name: {{ include "infraspend.fullname" . }}
  labels:
    {{- include "infraspend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infraspend.selectorLabels" . | nindent 4 }}
