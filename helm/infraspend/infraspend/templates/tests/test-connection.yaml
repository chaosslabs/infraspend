apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infraspend.fullname" . }}-test-connection"
  labels:
    {{- include "infraspend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infraspend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
